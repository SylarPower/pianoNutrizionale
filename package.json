{
  "name": "piano-nutrizionale",
  "version": "4.1.0",
  "private": true,
  "description": "PWA nutrizionale personale con console SaaS multi-tenant.",
  "scripts": {
    "test": "node --test test/*.test.js",
    "test:functions": "npm --prefix functions test",
    "test:rules": "firebase emulators:exec --only firestore --project piano-nutrizionale-test \"node --test test/firestore-rules.rules-test.js\"",
    "test:all": "npm test && npm run test:functions && npm run syntax && npm run smoke",
    "smoke": "node test/smoke-app.js",
    "syntax": "node --check js/domain.js && node --check js/saas-config.js && node --check js/saas.js && node --check js/data.js && node --check js/prices.js && node --check js/firebase.js && node --check js/admin.js && node --check js/app.js && node --check sw.js && npm --prefix functions run syntax"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase-tools": "^15.29.0"
  }
}
